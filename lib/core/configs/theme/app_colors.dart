import 'package:flutter/material.dart';

class AppColors {
  // Light Theme Colors
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightCardBackground = Color(0xFFF5F5F5);
  static const lightInputFieldBackground = Color(0xFFEFEFEF);
  static const lightInputText = Color(0xFF000000);
  static const lightPlaceholderText = Color(0xFF9E9E9E);
  static const lightPrimaryButton = Color(0xFF036A3B);
  static const lightButtonText = Color(0xFFFFFFFF);
  static const lightCancelButton = Color(0xFFCCCCCC);
  static const lightLinkText = Color(0xFF007F4E);

  // Dark Theme Colors
  static const darkBackground = Color(0xFF121212);
  static const darkCardBackground = Color(0xFF1F1F1F);
  static const darkInputFieldBackground = Color(0xFF2C2C2E);
  static const darkInputText = Color(0xFFFFFFFF);
  static const darkPlaceholderText = Color(0xFFB0B0B0);
  static const darkPrimaryButton = Color(0xFF007F4E);
  static const darkButtonText = Color(0xFFFFFFFF);
  static const darkCancelButton = Color(0xFF2E2E2E);
  static const darkLinkText = Color(0xFF00A865);
  static const darkAppButton = Color(0xff0025292e);
}
